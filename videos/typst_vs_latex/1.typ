#set page(
  width: 210mm,
  height: 297mm,
  margin: (top: 25mm, bottom: 25mm, left: 25mm, right: 25mm)
)

#set text(
  font: "Inter",
  size: 11pt
)

#set heading(numbering: none)

#let accent = rgb("#4f46e5")
#let muted = rgb("#6b7280")

// ===== Заголовок =====
#align(center)[
  #text(size: 28pt, weight: "bold", fill: accent)[
    Ghostfolio
  ]

  #v(8pt)

  #text(size: 14pt, fill: muted)[
    Privacy-first investment dashboard
  ]
]

#v(20pt)

// ===== Раздел =====
#heading(level: 2)[О проекте]

Ghostfolio — это **open-source платформа**, созданная для людей,
которые хотят управлять своими инвестициями **без слежки и рекламы**.

Она помогает:
- отслеживать портфель акций, ETF и криптовалют
- анализировать распределение активов
- видеть реальную чистую стоимость капитала
- принимать решения на основе данных, а не эмоций

#v(16pt)

// ===== Карточки =====
#grid(
  columns: (1fr, 1fr),
  gutter: 16pt,
)[
  #box(
    radius: 8pt,
    inset: 14pt,
    stroke: accent,
  )[
    #text(weight: "bold")[🔐 Конфиденциальность]
    #v(6pt)
    Все данные принадлежат только вам.
    Никакого трекинга, аналитики и рекламы.
  ]

  #box(
    radius: 8pt,
    inset: 14pt,
    stroke: accent,
  )[
    #text(weight: "bold")[📊 Аналитика]
    #v(6pt)
    Графики, распределение активов,
    история доходности и метрики риска.
  ]
]

#v(16pt)

#grid(
  columns: (1fr, 1fr),
  gutter: 16pt,
)[
  #box(
    radius: 8pt,
    inset: 14pt,
    stroke: accent,
  )[
    #text(weight: "bold")[🧠 Осознанные решения]
    #v(6pt)
    Понимай, куда идут твои деньги
    и как меняется стратегия со временем.
  ]

  #box(
    radius: 8pt,
    inset: 14pt,
    stroke: accent,
  )[
    #text(weight: "bold")[🌍 Self-hosted]
    #v(6pt)
    Можно развернуть на собственном сервере,
    например на Umbrel или VPS.
  ]
]

#v(24pt)

// ===== Подвал =====
#line(length: 100%, stroke: muted)

#v(8pt)

#align(center)[
  #text(size: 9pt, fill: muted)[
    © 2026 • Open Source • typst.app
  ]
]
